<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Scatch</title>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display'] bg-gray-50">
  <nav class="w-full fixed top-0 left-0 bg-white shadow-md px-6 py-3 flex items-center justify-between z-50">
    <h3 class="text-2xl font-extrabold text-blue-600 tracking-wide">Scatch</h3>

    <% var LoggedIn = typeof LoggedIn !== 'undefined' ? LoggedIn : true %>
    <% if(LoggedIn){ %>
    <div class="flex space-x-6 text-sm font-medium">
      <a href="/shop" class="text-gray-700 hover:text-blue-600 transition">Shop</a>
      <a href="/cart" class="text-gray-700 hover:text-blue-600 transition">Cart</a>
      <a href="#" class="text-gray-700 hover:text-blue-600 transition">My Account</a>
      <a href="/user/logout" class="text-red-600 hover:text-red-800 transition">Logout</a>
    </div>
    <% } %>
  </nav>
</body>

</html>
